[project]
name = "valtrack"
version = "0.0.1"
description = "simplest metric solution this side of the number river"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi>=0.128.1",
    "sqlalchemy>=2.0.46",
    "sse-starlette>=3.2.0",
    "uvicorn>=0.40.0",
]

[tool.pyrefly]
